import re
import json
import logging
from typing import Dict, Any, Optional
from core.gemini import ask_gemini

logger = logging.getLogger("AutoScamShield")

async def analyze_repair_estimate(user_id: int, text_or_prompt: str, image_bytes: Optional[bytes] = None) -> Dict[str, Any]:
    prompt = f"""Ты — независимый судебный автоэксперт, шеф-механик и борец с навязанными услугами в автосервисах.
Данные заказ-наряда / сметы / список работ автосервиса:
'{text_or_prompt}'

Проведи честный аудит сметы:
1. Выяви навязанные, бессмысленные и разводные позиции.
2. Подтверди реально важные работы для безопасности.
3. Оцени адекватность цен на работы и запчасти по рынку Санкт-Петербурга.

СТРУКТУРА JSON:
1. "overall_verdict": Общий вердикт (например: «⚠️ Завышение чека на ~40%! Есть 3 навязанных позиции»)
2. "scam_items": Список навязанных услуг/работ, от которых нужно отказаться:
   - "item": Название работы
   - "price_in_bill": Цена в счете
   - "why_scam": Почему это развод (например: промывка форсунок без диагностики, очистка тормозов за 3000р, замена исправных узлов)
3. "critical_items": Реально важные работы (на что согласиться)
4. "fair_price_estimate": Справедливая итоговая цена этих работ по рынку
5. "negotiation_script": Готовая фраза мастеру-приемщику, чтобы он вычеркнул развод и снизил цену.

Верни ответ СТРОГО в формате JSON:
{{
  "overall_verdict": "⚠️ В смете обнаружены навязанные услуги на сумму ~18 500 ₽. Рекомендуем вычеркнуть 3 позиции.",
  "scam_items": [
    {{
      "item": "Промывка масляной системы и ультразвук форсунок",
      "price_in_bill": "6 500 ₽",
      "why_scam": "Маркетинговая услуга без показаний. Современные синтетические масла моют мотор сами, а промывка может забить маслоприемник."
    }}
  ],
  "critical_items": ["Замена передних тормозных колодок и дисков (износ >80%)", "Замена моторного масла и масляного фильтра"],
  "fair_price_estimate": "Честная стоимость необходимых работ с запчастями: 14 000 – 17 000 ₽ вместо 35 000 ₽.",
  "negotiation_script": "💬 Скажите мастеру: «Промывку и доп. очистку вычеркиваем, делаем только замену колодок и ТО. Заказ-наряд согласую без допов»."
}}
"""
    resp = await ask_gemini(user_id, prompt, image_bytes=image_bytes)
    try:
        m = re.search(r"\{.*\}", resp, re.DOTALL)
        if m:
            return json.loads(m.group(0))
    except Exception as e:
        logger.error(f"Error parsing auto scam json: {e}")

    return {
        "overall_verdict": "Анализ сметы завершен.",
        "scam_items": [],
        "critical_items": ["Плановое техническое обслуживание"],
        "fair_price_estimate": "Цены в пределах рыночной нормы.",
        "negotiation_script": "Уточните гарантию на выполненные работы и детали."
    }
